export enum AccountType {
    Bank = "Bank Account",
    Bitcoin = "Bitcoin Wallet",
    VirtualBitcoin = "Virtual Bitcoin",
    All = "All",
  }
  
export enum CurrencyType {
    USD = "USD",
    BTC = "BTC",
}

export enum PendingFirstChannelsStatus {
  pending = "pending",
  opened = "opened",
  noChannel = "noChannel",
}
